# Terraform applying to Digital Ocean

*(just another give a try personal self-training)*

## Requirements:

* I use latest terraform:
```
$ terraform --version

Terraform v0.14.3
```
* Init modules: `terraform init`
